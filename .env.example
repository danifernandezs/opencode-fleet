# Domain for Caddy TLS (e.g. oc.yourdomain.com)
DOMAIN=oc.yourdomain.com

# Password for opencode server basic auth
# Generate one with: openssl rand -base64 24
OPENCODE_SERVER_PASSWORD=

# Socket-proxy log level (debug, info, notice, warning, err)
LOG_LEVEL=info
